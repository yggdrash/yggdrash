
logging:
  level:
    io.yggdrash: debug

yggdrash.node:
  grpc.host: "localhost"
  grpc.port: 32918
  max-peers: 25

management:
  endpoint.health.show-details: always
  health:
    diskspace:
      enabled: false

spring:
  output:
    ansi:
      enabled: always
